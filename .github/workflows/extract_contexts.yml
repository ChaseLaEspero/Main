name: Extract portal endpoint contexts

on:
  push:
    branches: [main]
    paths:
      - extract_endpoint_contexts.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python extract_endpoint_contexts.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add custom_portal_probe/*contexts* || true
          if ! git diff --cached --quiet; then
            git commit -m "Store portal endpoint contexts"
            git pull --rebase
            git push
          fi
